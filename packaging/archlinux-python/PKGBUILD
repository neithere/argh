# Maintainer: Andrey Mikhaylenko <neithere at gmail dot com>
# Contributor: Fabien Devaux <fdev31 at gmail dot com>
pkgname=python-argh
pkgver=0.19.0
pkgrel=2       # XXX don't forget to reset on version change
pkgdesc="A simple argparse wrapper"
arch=(any)
url="http://bitbucket.org/neithere/argh/"
license=('LGPL3')
depends=('python>=3.2')
makedepends=('python-distribute')
provides=()
conflicts=()
replaces=()
backup=()
options=(!emptydirs)
install=
source=(http://pypi.python.org/packages/source/a/argh/argh-${pkgver}.tar.gz)
md5sums=('8eca336ecdb8512252c2b5f5a824e400')

build() {
   cd "${srcdir}/argh-${pkgver}"
   python setup.py install --root="${pkgdir}" --optimize=1 || exit 1
}
